<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/submit_header"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">
    <View
        android:id="@+id/statusBar1"
        android:layout_width="57dp"
        android:layout_height="25dp"
        android:background="@color/white"
        android:visibility="gone" />

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/head_height"
        android:background="@color/white">

        <TextView
            android:id="@+id/submit_head_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="新增报修"
            android:textColor="@color/black"
            android:textSize="20sp"
             />

        <FrameLayout
            android:id="@+id/submit_order_right_container"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_alignParentRight="true"
            android:paddingRight="@dimen/padding_right"
            android:visibility="visible">

            <TextView
                android:id="@+id/submit_order_text_right"
                android:layout_width="match_parent"
                android:layout_height="30dp"
                android:layout_gravity="center"
                android:text="提交"
                android:textColor="#3d44d2"
                android:textSize="18sp" />

        </FrameLayout>
        <FrameLayout
            android:id="@+id/submit_order_left_container"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_alignParentLeft="true"
            android:paddingRight="@dimen/padding_left"
            android:visibility="visible">

            <ImageView
                android:id="@+id/submit_order_img_left"
                android:layout_width="25dp"
                android:layout_height="25dp"
                android:layout_gravity="center"
                app:srcCompat="@mipmap/left_back" />

        </FrameLayout>
    </RelativeLayout>
</LinearLayout>
